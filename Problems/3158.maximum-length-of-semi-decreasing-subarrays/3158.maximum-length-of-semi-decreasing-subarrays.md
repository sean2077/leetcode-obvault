---
tags:
  - leetcode/problem
questionId: '3158'
title: Maximum Length of Semi-Decreasing Subarrays
translatedTitle: 最长半递减数组
titleSlug: maximum-length-of-semi-decreasing-subarrays
aliases:
  - Maximum Length of Semi-Decreasing Subarrays
  - maximum-length-of-semi-decreasing-subarrays
  - 最长半递减数组
lcLink: https://leetcode.com/problems/maximum-length-of-semi-decreasing-subarrays/
lcTopics:
  - '[[array]]'
  - '[[hash-table]]'
  - '[[sorting]]'
lcDifficulty: Medium
lcAcRate: 70.9%
similarQuestions: []
grade: ⭐⭐⭐⭐
likes: 120
dislikes: 16
favorites: []
solutions: []
notes: []
cssclasses: []
created: 2024-08-06 00:40
updated:
---

**Nav:** << previous: [[3156.rolling-average-steps]] | next: [[3160.calculate-orders-within-each-interval]] >>

---

## Description

~~~tabs
tab: English

No description

> [!tip]- Hint 1
> First, solve the problem assuming <code>nums</code> contains distinct values.



> [!tip]- Hint 2
> Make a new array with each element being the pair <code>(nums[i], i)</code> for every <code>i</code> and call it <code>num_ind</code>.



> [!tip]- Hint 3
> Sort <code>num_ind</code> in decreasing order.



> [!tip]- Hint 4
> Iterate over <code>num_ind</code> and store a variable that represents the minimum index (i.e. min of <code>num_ind[i].second</code>) that has been iterated until now. Call it <code>min_index</code>



> [!tip]- Hint 5
> Now if you are currently on pair <code>(nums[x], x)</code>, then <code>ans = max(ans, min_index - x)</code>.



> [!tip]- Hint 6
> Now try to remove the first assumption.



---

[submissions](https://leetcode.com/problems/maximum-length-of-semi-decreasing-subarrays/submissions/) | [solutions](https://leetcode.com/problems/maximum-length-of-semi-decreasing-subarrays/solutions/)


tab: 中文

暂无描述

> [!tip]- 提示 1
> First, solve the problem assuming <code>nums</code> contains distinct values.




> [!tip]- 提示 2
> Make a new array with each element being the pair <code>(nums[i], i)</code> for every <code>i</code> and call it <code>num_ind</code>.




> [!tip]- 提示 3
> Sort <code>num_ind</code> in decreasing order.




> [!tip]- 提示 4
> Iterate over <code>num_ind</code> and store a variable that represents the minimum index (i.e. min of <code>num_ind[i].second</code>) that has been iterated until now. Call it <code>min_index</code>




> [!tip]- 提示 5
> Now if you are currently on pair <code>(nums[x], x)</code>, then <code>ans = max(ans, min_index - x)</code>.




> [!tip]- 提示 6
> Now try to remove the first assumption.




---

[提交记录](https://leetcode.cn/problems/maximum-length-of-semi-decreasing-subarrays/submissions/) | [题解](https://leetcode.cn/problems/maximum-length-of-semi-decreasing-subarrays/solution/)


~~~

## Code Snippets

~~~tabs
~~~

## Similar Questions

```dataviewjs
const currentFilePath = dv.current().file.path;
dv.table(
    ["题号", "标题", "标题(中)", "分类",  "难度", "通过率", "评分", "解法", "笔记", "收藏"],
    dv.pages("#leetcode/problem")
        .filter(p => p.similarQuestions && p.similarQuestions.some(q => q.path === currentFilePath))
        .map((p) => [p.file.link, p.title, p.translatedTitle, p.lcTopics, p.lcDifficulty, p.lcAcRate, p.grade, p.solutions, p.notes, p.favorites])
);
```

## Solutions

```dataviewjs
const currentDir = dv.current().file.folder;
const solutionPages = dv.pages(`"${currentDir}" and #leetcode/solution`);
dv.table(
    ["解法", "描述", "编程语言", "评分", "相关链接", "最后更新"],
    solutionPages
        .map((p) => [p.file.link, p.desc, p.program_language,p.grade, p.related_links, p.updated])
);
//  更新 solutions 元信息
const currentFile = app.vault.getAbstractFileByPath(dv.current().file.path);
let solutionList = solutionPages.map(p => `[[${p.file.link.path}|${p.file.name}]]`).array();
await app.fileManager.processFrontMatter(currentFile, (fm) => {
  fm["solutions"] = solutionList;
});
```

## Notes

```dataviewjs
const currentFilePath = dv.current().file.path;
let notePages = dv.pages(`#leetcode/note`)
	.filter(p => p.related_questions && p.related_questions.some(q => q.path === currentFilePath));
dv.table(["笔记", "标题", "描述", "评分", "最后更新"],
  notePages.map(p => [p.file.link, p.title, p.desc, p.grade, p.updated])
);
// 更新 notes 元信息
const currentFile = app.vault.getAbstractFileByPath(currentFilePath);
let noteList = notePages.map(p => `[[${p.file.link.path}|${p.file.name}]]`).array();
await app.fileManager.processFrontMatter(currentFile, (fm) => {
  fm["notes"] = noteList;
});
```
          
