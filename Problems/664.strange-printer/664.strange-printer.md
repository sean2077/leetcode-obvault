---
tags:
  - leetcode/problem
questionId: '664'
title: Strange Printer
translatedTitle: 奇怪的打印机
titleSlug: strange-printer
aliases:
  - Strange Printer
  - strange-printer
  - 奇怪的打印机
lcLink: https://leetcode.com/problems/strange-printer/
lcTopics:
  - '[[string]]'
  - '[[dynamic-programming]]'
lcDifficulty: Hard
lcAcRate: 55.0%
similarQuestions:
  - '[[546.remove-boxes|546.移除盒子]]'
  - '[[1591.strange-printer-ii|1591.奇怪的打印机 II]]'
grade: ⭐⭐⭐⭐
likes: 2546
dislikes: 210
favorites: []
solutions: []
notes: []
cssclasses: []
created: 2024-08-13 00:09
updated:
---

**Nav:** << previous: [[663.equal-tree-partition]] | next: [[665.non-decreasing-array]] >>

---

## Description

~~~tabs
tab: English

<p>There is a strange printer with the following two special properties:</p>

<ul>
	<li>The printer can only print a sequence of <strong>the same character</strong> each time.</li>
	<li>At each turn, the printer can print new characters starting from and ending at any place and will cover the original existing characters.</li>
</ul>

<p>Given a string <code>s</code>, return <em>the minimum number of turns the printer needed to print it</em>.</p>

<p>&nbsp;</p>
<p><strong class="example">Example 1:</strong></p>

<pre>
<strong>Input:</strong> s = &quot;aaabbb&quot;
<strong>Output:</strong> 2
<strong>Explanation:</strong> Print &quot;aaa&quot; first and then print &quot;bbb&quot;.
</pre>

<p><strong class="example">Example 2:</strong></p>

<pre>
<strong>Input:</strong> s = &quot;aba&quot;
<strong>Output:</strong> 2
<strong>Explanation:</strong> Print &quot;aaa&quot; first and then print &quot;b&quot; from the second place of the string, which will cover the existing character &#39;a&#39;.
</pre>

<p>&nbsp;</p>
<p><strong>Constraints:</strong></p>

<ul>
	<li><code>1 &lt;= s.length &lt;= 100</code></li>
	<li><code>s</code> consists of lowercase English letters.</li>
</ul>


---

[submissions](https://leetcode.com/problems/strange-printer/submissions/) | [solutions](https://leetcode.com/problems/strange-printer/solutions/)


tab: 中文

<p>有台奇怪的打印机有以下两个特殊要求：</p>

<ul>
	<li>打印机每次只能打印由 <strong>同一个字符</strong> 组成的序列。</li>
	<li>每次可以在从起始到结束的任意位置打印新字符，并且会覆盖掉原来已有的字符。</li>
</ul>

<p>给你一个字符串 <code>s</code> ，你的任务是计算这个打印机打印它需要的最少打印次数。</p>
&nbsp;

<p><strong>示例 1：</strong></p>

<pre>
<strong>输入：</strong>s = "aaabbb"
<strong>输出：</strong>2
<strong>解释：</strong>首先打印 "aaa" 然后打印 "bbb"。
</pre>

<p><strong>示例 2：</strong></p>

<pre>
<strong>输入：</strong>s = "aba"
<strong>输出：</strong>2
<strong>解释：</strong>首先打印 "aaa" 然后在第二个位置打印 "b" 覆盖掉原来的字符 'a'。
</pre>

<p>&nbsp;</p>

<p><strong>提示：</strong></p>

<ul>
	<li><code>1 &lt;= s.length &lt;= 100</code></li>
	<li><code>s</code> 由小写英文字母组成</li>
</ul>


---

[提交记录](https://leetcode.cn/problems/strange-printer/submissions/) | [题解](https://leetcode.cn/problems/strange-printer/solution/)


~~~

## Code Snippets

~~~tabs
tab: C++

```cpp
class Solution {
public:
    int strangePrinter(string s) {

    }
};
```

tab: Java

```java
class Solution {
    public int strangePrinter(String s) {

    }
}
```

tab: Python

```python
class Solution(object):
    def strangePrinter(self, s):
        """
        :type s: str
        :rtype: int
        """
```

tab: Python3

```python
class Solution:
    def strangePrinter(self, s: str) -> int:
```

tab: C

```c
int strangePrinter(char* s) {
    
}
```

tab: C#

```csharp
public class Solution {
    public int StrangePrinter(string s) {

    }
}
```

tab: JavaScript

```javascript
/**
 * @param {string} s
 * @return {number}
 */
var strangePrinter = function(s) {

};
```

tab: TypeScript

```typescript
function strangePrinter(s: string): number {
    
};
```

tab: PHP

```php
class Solution {

    /**
     * @param String $s
     * @return Integer
     */
    function strangePrinter($s) {

    }
}
```

tab: Swift

```swift
class Solution {
    func strangePrinter(_ s: String) -> Int {

    }
}
```

tab: Kotlin

```kotlin
class Solution {
    fun strangePrinter(s: String): Int {

    }
}
```

tab: Dart

```dart
class Solution {
  int strangePrinter(String s) {
    
  }
}
```

tab: Go

```go
func strangePrinter(s string) int {

}
```

tab: Ruby

```ruby
# @param {String} s
# @return {Integer}
def strange_printer(s)

end
```

tab: Scala

```scala
object Solution {
    def strangePrinter(s: String): Int = {

    }
}
```

tab: Rust

```rust
impl Solution {
    pub fn strange_printer(s: String) -> i32 {

    }
}
```

tab: Racket

```racket
(define/contract (strange-printer s)
  (-> string? exact-integer?)
  )
```

tab: Erlang

```erlang
-spec strange_printer(S :: unicode:unicode_binary()) -> integer().
strange_printer(S) ->
  .
```

tab: Elixir

```elixir
defmodule Solution do
  @spec strange_printer(s :: String.t) :: integer
  def strange_printer(s) do
    
  end
end
```

~~~

## Similar Questions

```dataviewjs
const currentFilePath = dv.current().file.path;
dv.table(
    ["题号", "标题", "标题(中)", "分类",  "难度", "通过率", "评分", "解法", "笔记", "收藏"],
    dv.pages("#leetcode/problem")
        .filter(p => p.similarQuestions && p.similarQuestions.some(q => q.path === currentFilePath))
        .map((p) => [p.file.link, p.title, p.translatedTitle, p.lcTopics, p.lcDifficulty, p.lcAcRate, p.grade, p.solutions, p.notes, p.favorites])
);
```

## Solutions

```dataviewjs
const currentDir = dv.current().file.folder;
const solutionPages = dv.pages(`"${currentDir}" and #leetcode/solution`);
dv.table(
    ["解法", "描述", "编程语言", "评分", "相关链接", "最后更新"],
    solutionPages
        .map((p) => [p.file.link, p.desc, p.program_language,p.grade, p.related_links, p.updated])
);
//  更新 solutions 元信息
const currentFile = app.vault.getAbstractFileByPath(dv.current().file.path);
let solutionList = solutionPages.map(p => `[[${p.file.link.path}|${p.file.name}]]`).array();
await app.fileManager.processFrontMatter(currentFile, (fm) => {
  fm["solutions"] = solutionList;
});
```

## Notes

```dataviewjs
const currentFilePath = dv.current().file.path;
let notePages = dv.pages(`#leetcode/note`)
	.filter(p => p.related_questions && p.related_questions.some(q => q.path === currentFilePath));
dv.table(["笔记", "标题", "描述", "评分", "最后更新"],
  notePages.map(p => [p.file.link, p.title, p.desc, p.grade, p.updated])
);
// 更新 notes 元信息
const currentFile = app.vault.getAbstractFileByPath(currentFilePath);
let noteList = notePages.map(p => `[[${p.file.link.path}|${p.file.name}]]`).array();
await app.fileManager.processFrontMatter(currentFile, (fm) => {
  fm["notes"] = noteList;
});
```
          
