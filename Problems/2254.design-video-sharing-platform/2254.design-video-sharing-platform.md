---
tags:
  - leetcode/problem
questionId: '2254'
title: Design Video Sharing Platform
translatedTitle: 设计视频共享平台
titleSlug: design-video-sharing-platform
aliases:
  - Design Video Sharing Platform
  - design-video-sharing-platform
  - 设计视频共享平台
lcLinks:
  - https://leetcode.com/problems/design-video-sharing-platform/
  - https://leetcode.cn/problems/design-video-sharing-platform/
lcTopics:
  - '[[stack]]'
  - '[[design]]'
  - '[[hash-table]]'
  - '[[ordered-set]]'
lcDifficulty: Hard
lcAcRate: 64.2%
similarQuestions:
  - '[[1348.tweet-counts-per-frequency|1348.推文计数]]'
  - '[[1472.design-browser-history|1472.设计浏览器历史记录]]'
  - '[[2456.most-popular-video-creator|2456.最流行的视频创作者]]'
grade: ⭐⭐⭐⭐
likes: 83
dislikes: 10
favorites: []
solutions: []
notes: []
cssclasses: []
created: 2024-08-13 00:28
updated:
---

**Nav:** << previous: [[2253.dynamic-unpivoting-of-a-table]] | next: [[2255.count-prefixes-of-a-given-string]] >>

---

## Description

~~~tabs
tab: English

No description

> [!tip]- Hint 1
> How can we keep track of the videoId’s of removed videos?



> [!tip]- Hint 2
> When you remove a video, keep track of its videoId in an array called usedId. When a new video is uploaded, first check if usedId contains any elements, if it does, use the smallest value in usedId.



---

[submissions](https://leetcode.com/problems/design-video-sharing-platform/submissions/) | [solutions](https://leetcode.com/problems/design-video-sharing-platform/solutions/)


tab: 中文

暂无描述

> [!tip]- 提示 1
> How can we keep track of the videoId’s of removed videos?




> [!tip]- 提示 2
> When you remove a video, keep track of its videoId in an array called usedId. When a new video is uploaded, first check if usedId contains any elements, if it does, use the smallest value in usedId.




---

[提交记录](https://leetcode.cn/problems/design-video-sharing-platform/submissions/) | [题解](https://leetcode.cn/problems/design-video-sharing-platform/solution/)


~~~

## Code Snippets

~~~tabs
~~~

## Similar Questions

```dataviewjs
const currentFilePath = dv.current().file.path;
dv.table(
    ["题号", "标题", "标题(中)", "分类",  "难度", "通过率", "评分", "解法", "笔记", "收藏"],
    dv.pages("#leetcode/problem")
        .filter(p => p.similarQuestions && p.similarQuestions.some(q => q.path === currentFilePath))
        .map((p) => [p.file.link, p.title, p.translatedTitle, p.lcTopics, p.lcDifficulty, p.lcAcRate, p.grade, p.solutions, p.notes, p.favorites])
);
```

## Solutions

```dataviewjs
const currentDir = dv.current().file.folder;
const solutionPages = dv.pages(`"${currentDir}" and #leetcode/solution`);
dv.table(
    ["解法", "描述", "编程语言", "评分", "相关链接", "最后更新"],
    solutionPages
        .map((p) => [p.file.link, p.desc, p.program_language,p.grade, p.related_links, p.updated])
);
//  更新 solutions 元信息
const currentFile = app.vault.getAbstractFileByPath(dv.current().file.path);
let solutionList = solutionPages.map(p => `[[${p.file.link.path}|${p.file.name}]]`).array();
await app.fileManager.processFrontMatter(currentFile, (fm) => {
  fm["solutions"] = solutionList;
});
```

## Notes

```dataviewjs
const currentFilePath = dv.current().file.path;
let notePages = dv.pages(`#leetcode/note`)
	.filter(p => p.related_questions && p.related_questions.some(q => q.path === currentFilePath));
dv.table(["笔记", "标题", "描述", "评分", "最后更新"],
  notePages.map(p => [p.file.link, p.title, p.desc, p.grade, p.updated])
);
// 更新 notes 元信息
const currentFile = app.vault.getAbstractFileByPath(currentFilePath);
let noteList = notePages.map(p => `[[${p.file.link.path}|${p.file.name}]]`).array();
await app.fileManager.processFrontMatter(currentFile, (fm) => {
  fm["notes"] = noteList;
});
```
          
