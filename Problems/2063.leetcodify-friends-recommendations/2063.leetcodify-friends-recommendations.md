---
tags:
  - leetcode/problem
questionId: '2063'
title: Leetcodify Friends Recommendations
translatedTitle: Leetcodify 好友推荐
titleSlug: leetcodify-friends-recommendations
aliases:
  - Leetcodify Friends Recommendations
  - leetcodify-friends-recommendations
  - Leetcodify 好友推荐
lcLink: https://leetcode.com/problems/leetcodify-friends-recommendations/
lcTopics:
  - '[[database]]'
lcDifficulty: Hard
lcAcRate: 27.0%
similarQuestions:
  - '[[2064.leetcodify-similar-friends|2064.兴趣相同的朋友]]'
  - '[[2097.strong-friendship|2097.坚定的友谊]]'
grade: ⭐
likes: 66
dislikes: 60
favorites: []
solutions: []
notes: []
cssclasses: []
created: 2024-08-06 00:40
updated:
---

**Nav:** << previous: [[2062.game-of-nim]] | next: [[2064.leetcodify-similar-friends]] >>

---

## Description

~~~tabs
tab: English

No description

---

[submissions](https://leetcode.com/problems/leetcodify-friends-recommendations/submissions/) | [solutions](https://leetcode.com/problems/leetcodify-friends-recommendations/solutions/)


tab: 中文

暂无描述

---

[提交记录](https://leetcode.cn/problems/leetcodify-friends-recommendations/submissions/) | [题解](https://leetcode.cn/problems/leetcodify-friends-recommendations/solution/)


~~~

## Code Snippets

~~~tabs
~~~

## Similar Questions

```dataviewjs
const currentFilePath = dv.current().file.path;
dv.table(
    ["题号", "标题", "标题(中)", "分类",  "难度", "通过率", "评分", "解法", "笔记", "收藏"],
    dv.pages("#leetcode/problem")
        .filter(p => p.similarQuestions && p.similarQuestions.some(q => q.path === currentFilePath))
        .map((p) => [p.file.link, p.title, p.translatedTitle, p.lcTopics, p.lcDifficulty, p.lcAcRate, p.grade, p.solutions, p.notes, p.favorites])
);
```

## Solutions

```dataviewjs
const currentDir = dv.current().file.folder;
const solutionPages = dv.pages(`"${currentDir}" and #leetcode/solution`);
dv.table(
    ["解法", "描述", "编程语言", "评分", "相关链接", "最后更新"],
    solutionPages
        .map((p) => [p.file.link, p.desc, p.program_language,p.grade, p.related_links, p.updated])
);
//  更新 solutions 元信息
const currentFile = app.vault.getAbstractFileByPath(dv.current().file.path);
let solutionList = solutionPages.map(p => `[[${p.file.link.path}|${p.file.name}]]`).array();
await app.fileManager.processFrontMatter(currentFile, (fm) => {
  fm["solutions"] = solutionList;
});
```

## Notes

```dataviewjs
const currentFilePath = dv.current().file.path;
let notePages = dv.pages(`#leetcode/note`)
	.filter(p => p.related_questions && p.related_questions.some(q => q.path === currentFilePath));
dv.table(["笔记", "标题", "描述", "评分", "最后更新"],
  notePages.map(p => [p.file.link, p.title, p.desc, p.grade, p.updated])
);
// 更新 notes 元信息
const currentFile = app.vault.getAbstractFileByPath(currentFilePath);
let noteList = notePages.map(p => `[[${p.file.link.path}|${p.file.name}]]`).array();
await app.fileManager.processFrontMatter(currentFile, (fm) => {
  fm["notes"] = noteList;
});
```
          
